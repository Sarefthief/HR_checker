<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saref.hrchecker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.saref.hrchecker.features.events.presentation.EventsActivity"
            android:label="@string/eventsActivityTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.saref.hrchecker.features.members.presentation.MembersActivity"
            android:launchMode="singleTask" />
        <activity android:name="com.saref.hrchecker.features.memberInfo.presentation.MemberInfoActivity" />
        <activity android:name="com.saref.hrchecker.features.statistic.presentation.StatisticActivity"></activity>
    </application>

</manifest>